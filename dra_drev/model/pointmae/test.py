"""
Legacy / local experiment script (previously referenced a private project tree).

Training and evaluation entry points live at the repository root:

    python DRA_train.py --help
    python DevNet_train.py --help

Do not import this module from the training pipeline; it is kept only as a stub
for optional local debugging.
"""
